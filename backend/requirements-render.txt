# 老人短视频虚假信息检测系统 - Render 部署依赖（轻量版）
# 不含 PyTorch/Transformers（太重），AI 检测使用 TF-IDF 模型 + 规则引擎

# === Web框架 ===
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# === 数据验证 ===
pydantic==2.5.0
pydantic-settings==2.1.0

# === HTTP客户端（QClaw webhook 推送用）===
httpx==0.25.2

# === 环境变量 ===
python-dotenv==1.0.0

# === 日志 ===
loguru>=0.7.0

# === 轻量 AI 模型 ===
scikit-learn>=1.3.0
joblib>=1.3.0
numpy>=1.24.0
